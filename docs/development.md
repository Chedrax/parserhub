# Development Rules

This document defines the development conventions and workflow used in the ParserHub project.

The goal is to keep the codebase consistent, maintainable, and easy to work with as the project grows.

---

## Development Environment

### Requirements

The following tools are required for local development:

* Python 3.12
* uv
* Docker
* Git

Node.js will be required when the frontend is introduced.

PostgreSQL is provided through Docker for local development.

The backend requires Python 3.12 specifically, as defined in `backend/pyproject.toml`:

```text
>=3.12,<3.13
```

---

## Project Setup

Clone the repository:

```bash
git clone <repository-url>
cd parserhub
```

Create and synchronize the backend environment:

```bash
cd backend
uv sync
```

Return to the repository root:

```bash
cd ..
```

Create the local environment file:

```text
.env
```

using:

```text
.env.example
```

The `.env` file contains local configuration and must not be committed to Git.

---

## Environment Configuration

ParserHub uses `pydantic-settings` for application configuration.

Application configuration is loaded from environment variables.

The example configuration is stored in:

```text
.env.example
```

For local development, create a `.env` file in the repository root.

The main configuration variables are:

```env
ENVIRONMENT=development

DATABASE_URL=postgresql+asyncpg://parserhub:change-me@localhost:5432/parserhub

POSTGRES_USER=parserhub
POSTGRES_PASSWORD=change-me
POSTGRES_DB=parserhub

SECRET_KEY=change-me

ACCESS_TOKEN_EXPIRE_MINUTES=30
```

The `.env` file must not be committed to Git.

Do not store real secrets or credentials in `.env.example`.

Environment-specific configuration should be provided through environment variables rather than hardcoded in application code.

---

## Local Infrastructure

ParserHub uses Docker for local infrastructure services.

### PostgreSQL

PostgreSQL is provided through the project's Docker Compose configuration.

The Compose configuration is located at:

```text
compose.yaml
```

Docker Compose commands must be executed from the repository root because `compose.yaml` is located there.

Start PostgreSQL:

```bash
docker compose up -d
```

Check the container status:

```bash
docker compose ps
```

The PostgreSQL container should report a healthy status before running operations that require the database.

View PostgreSQL logs:

```bash
docker compose logs postgres
```

Follow PostgreSQL logs:

```bash
docker compose logs -f postgres
```

Stop the PostgreSQL container:

```bash
docker compose down
```

Stop the container and remove the associated local database volume:

```bash
docker compose down -v
```

The `-v` option permanently removes the local PostgreSQL data stored in the Docker volume. Use it only when the local database should be reset.

The PostgreSQL service uses the following variables from `.env`:

```text
POSTGRES_USER
POSTGRES_PASSWORD
POSTGRES_DB
```

The backend connects to PostgreSQL through:

```text
DATABASE_URL
```

---

## Database Development

ParserHub uses PostgreSQL as its database and SQLAlchemy for database access.

Database schema changes are managed through Alembic migrations.

Alembic is configured in:

```text
backend/alembic.ini
```

Migration files are stored in:

```text
backend/alembic/versions/
```

### Applying Migrations

Start PostgreSQL first:

```bash
docker compose up -d
```

Then from the `backend` directory:

```bash
uv run alembic upgrade head
```

This applies all pending migrations.

### Checking Migration Status

```bash
uv run alembic current
```

### Creating a Migration

After changing SQLAlchemy models, create a new migration:

```bash
uv run alembic revision --autogenerate -m "describe change"
```

Generated migrations must be reviewed manually before they are applied.

Do not assume that autogenerated migrations are always correct.

### Downgrading a Migration

To revert the most recent migration:

```bash
uv run alembic downgrade -1
```

Database migrations must be committed together with the model changes they represent.

---

## Running the Backend

The backend application is located in:

```text
backend/src/parserhub/
```

The FastAPI application entry point is:

```text
backend/src/parserhub/main.py
```

From the repository root:

```bash
cd backend
uv run uvicorn parserhub.main:app --reload
```

The `--reload` option enables automatic reloading during local development.

The FastAPI application provides automatically generated API documentation.

The API is versioned under:

```text
/api/v1
```

The current API router is located at:

```text
backend/src/parserhub/api/v1/router.py
```

---

## Authentication

ParserHub uses JWT access tokens for authentication.

Authentication is implemented through the API, service, repository, security, and dependency layers.

### Registering a User

Users can register through the authentication API.

### Logging In

After successful authentication, the API returns an access token.

### Authenticated Requests

Protected endpoints require the access token to be sent using the Bearer authentication scheme:

```text
Authorization: Bearer <access-token>
```

Authentication endpoints and the current API contract can be explored through the FastAPI API documentation.

The architectural design of authentication is documented in:

```text
docs/architecture.md
```

---

## Testing

The backend uses `pytest` and `pytest-asyncio` for testing.

Tests are located in:

```text
backend/tests/
```

The test suite contains unit and integration tests.

### Running All Tests

From the `backend` directory:

```bash
uv run pytest
```

### Verbose Test Output

```bash
uv run pytest -v
```

### Running a Specific Test File

For example:

```bash
uv run pytest tests/unit/core/test_config.py
```

### Running a Specific Test Directory

```bash
uv run pytest tests/unit
```

or:

```bash
uv run pytest tests/integration
```

Integration tests that require PostgreSQL must be run with the local PostgreSQL container running.

A typical test setup is therefore:

```bash
docker compose up -d
cd backend
uv run pytest
```

The pytest configuration is defined in:

```text
backend/pyproject.toml
```

---

## Project Structure

The repository is organized into the following main areas:

```text
parserhub/
├── .github/
│   └── workflows/
├── backend/
│   ├── alembic/
│   │   └── versions/
│   ├── src/
│   │   └── parserhub/
│   │       ├── api/
│   │       │   └── v1/
│   │       │       └── endpoints/
│   │       ├── core/
│   │       ├── db/
│   │       ├── models/
│   │       ├── repositories/
│   │       ├── schemas/
│   │       ├── services/
│   │       └── utils/
│   └── tests/
├── docs/
├── scripts/
├── compose.yaml
├── Dockerfile
├── .env.example
└── README.md
```

Detailed architectural responsibilities are documented in:

```text
docs/architecture.md
```

---

## Development Workflow

A typical local development workflow is:

### 1. Update the repository

Switch to the appropriate development branch and pull the latest changes.

```bash
git pull
```

### 2. Synchronize dependencies

From the repository root:

```bash
cd backend
uv sync
```

### 3. Start local infrastructure

From the repository root:

```bash
cd ..
docker compose up -d
```

### 4. Apply database migrations

Return to the backend directory:

```bash
cd backend
uv run alembic upgrade head
```

### 5. Start the backend

```bash
uv run uvicorn parserhub.main:app --reload
```

### 6. Make changes

Implement the required functionality while following the project's architecture and code style rules.

### 7. Run tests

```bash
uv run pytest
```

### 8. Run code quality checks

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy .
```

### 9. Run pre-commit checks

```bash
uv run pre-commit run --all-files
```

### 10. Commit the changes

Create a commit following the Conventional Commits rules described below.

---

## Architecture Rules

ParserHub follows a layered architecture.

1. The API layer is responsible for HTTP concerns only.

2. The API layer must not contain business logic.

3. Services contain application and business logic.

4. Database access must be handled through repositories.

5. Services should not depend directly on SQLAlchemy session implementation details.

6. Pydantic schemas are used for API input and output validation.

7. SQLAlchemy models represent database entities and must not be used directly as API contracts.

8. Parsers must implement the common `BaseParser` interface.

9. Infrastructure-specific code should remain isolated from the domain and application logic.

10. Dependencies should flow from higher-level application logic toward lower-level infrastructure.

Detailed architectural decisions are documented in:

```text
docs/architecture.md
```

---

## Python Code Style

The backend targets Python 3.12.

Use modern Python syntax where possible.

### Type Hints

Prefer built-in generic types:

```python
list[str]

dict[str, int]

tuple[int, str]

set[str]
```

Prefer the union operator:

```python
str | None

str | int
```

Avoid legacy typing aliases when an equivalent modern syntax is available:

```python
# Preferred
users: list[str]
username: str | None

# Avoid
from typing import List, Optional

users: List[str]
username: Optional[str]
```

The `typing` module should still be used when it provides functionality that does not have an equivalent built-in syntax:

```python
from typing import Literal, Protocol, TypeVar, TypedDict
```

---

## Tooling & Environment

The backend environment and development tools are managed with **uv**.

`uv` is the required package and environment manager for the backend.

Backend dependencies must be installed and synchronized using:

```bash
cd backend
uv sync
```

Backend development tools should be executed through `uv`:

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy .
uv run pytest
```

Do not rely on globally installed Python packages or tools for backend development.

This ensures that local development uses the versions declared in `pyproject.toml` and locked in `uv.lock`.

The `uv.lock` file must be committed to the repository and kept synchronized with `pyproject.toml`.

### Pre-commit Setup

ParserHub uses `pre-commit` for local automated checks.

The pre-commit configuration is stored in the repository root:

```text
.pre-commit-config.yaml
```

After cloning the repository, install the Git hooks:

```bash
uv run --directory backend pre-commit install
```

This step cannot be performed automatically by the repository configuration because Git hooks are installed into the local `.git/hooks` directory and are not tracked by Git.

The installed hook runs automatically before commits and validates:

* Ruff linting;
* Ruff formatting;
* commit message format.

The hooks can also be executed manually for the entire repository:

```bash
uv run --directory backend pre-commit run --all-files
```

If the repository is cloned on a new machine, the pre-commit hook must be installed again.

CI does not depend on the locally installed Git hooks.

GitHub Actions runs the required checks independently, so bypassing or missing local hooks does not bypass CI validation.

---

## Code Quality

### Backend

The backend uses:

* Ruff for linting and formatting;
* mypy for static type checking;
* pytest for testing;
* pytest-asyncio for asynchronous tests;
* pre-commit for local automated checks.

Before creating a commit, the following backend checks should pass:

```bash
cd backend

uv run ruff check .
uv run ruff format --check .
uv run mypy .
uv run pytest
```

The pre-commit hook automatically runs the configured local checks before a commit is created.

Developers should still run the full check suite manually before pushing changes.

The commit message is also validated by the pre-commit `commit-msg` hook according to the project's Conventional Commits rules.

### Frontend

The frontend is not currently part of the repository.

Frontend development tooling will be documented here when the frontend is introduced.

---

## Git Workflow

ParserHub uses the following long-lived branches:

```text
main
develop
```

### `main`

`main` contains production-ready code.

Direct commits to `main` should not be made.

Changes should be merged through a Pull Request.

### `develop`

`develop` contains the current development version of the project.

Feature and fix branches are normally created from `develop` and merged back into `develop`.

Changes should be reviewed through a Pull Request when practical.

---

## Branch Naming

Branches should follow one of the following formats:

```text
<type>/<issue-number>-<short-description>

<type>/<short-description>
```

When a branch is associated with a GitHub Issue, the issue number should be included.

Examples:

```text
feat/12-user-authentication

fix/27-login-validation

refactor/34-parser-registry

docs/41-development-guide

test/52-parser-service-tests

chore/18-update-dependencies

ci/23-improve-backend-checks
```

When a branch is not associated with a GitHub Issue, the issue number can be omitted:

```text
docs/update-readme

chore/update-uv

ci/update-workflow
```

Use lowercase words separated by hyphens.

The branch type should describe the purpose of the change.

### Branch Types

| Type       | Purpose                                      |
| ---------- | -------------------------------------------- |
| `feat`     | New functionality                            |
| `fix`      | Bug fix                                      |
| `refactor` | Code restructuring without changing behavior |
| `docs`     | Documentation changes                        |
| `test`     | Adding or modifying tests                    |
| `chore`    | Maintenance tasks                            |
| `ci`       | CI/CD configuration                          |
| `perf`     | Performance improvements                     |

---

## Commit Convention

ParserHub follows the **Conventional Commits** specification.

The basic format is:

```text
<type>(<scope>): <description>
```

Examples:

```text
feat(auth): add user registration

fix(auth): validate duplicate email

docs(development): document branch naming

refactor(parser): simplify parser registry

test(auth): add registration tests

chore(deps): update project dependencies

ci(github): run backend checks
```

### Commit Types

| Type       | Purpose                            |
| ---------- | ---------------------------------- |
| `feat`     | Introduces a new feature           |
| `fix`      | Fixes a bug                        |
| `docs`     | Documentation changes              |
| `refactor` | Code restructuring                 |
| `test`     | Adds or modifies tests             |
| `chore`    | Maintenance tasks                  |
| `ci`       | CI/CD changes                      |
| `perf`     | Performance improvements           |
| `build`    | Build system or dependency changes |

### Commit Header Rules

The commit header should:

* use lowercase for the type;
* use an optional scope when it adds useful context;
* use a short imperative description;
* not end with a period;
* describe what the commit does, not why it was done.

Good:

```text
feat(auth): add JWT authentication
```

Bad:

```text
Added JWT authentication.
```

Bad:

```text
feat: changes
```

Bad:

```text
feat: I added authentication because we need login
```

### Commit Scope

Use a scope when it makes the commit easier to understand.

Common scopes include:

```text
backend
frontend
auth
api
parser
database
github
docker
deps
docs
```

Examples:

```text
feat(backend): add health check endpoint

feat(auth): add password hashing

fix(api): handle invalid request body

docs(architecture): document service layer

ci(github): add backend test job

chore(deps): update FastAPI
```

---

## Issue References

When a commit is related to a GitHub Issue, the issue should be referenced when useful.

For example:

```text
feat(auth): add user registration (#12)
```

or in the commit body:

```text
feat(auth): add user registration

Refs #12
```

The branch name should also contain the issue number when possible:

```text
feat/12-user-registration
```

This keeps the relationship between the Issue, branch, Pull Request, and commits clear.

---

## Pull Requests

Pull Requests should:

1. Have a clear title following Conventional Commits.
2. Reference the related GitHub Issue.
3. Explain what was changed.
4. Explain any important implementation decisions.
5. Pass all required CI checks.
6. Avoid unrelated changes.

Example PR title:

```text
feat(auth): add user registration
```

The following can be used in the PR description when the Pull Request completely resolves an issue:

```text
Closes #12
```

---

## General Rules

* Do not commit secrets or credentials.
* Do not commit `.env` files containing real credentials.
* Do not use `print()` for application logging.
* Use the project's logging system.
* Do not disable linting or type checking to hide errors.
* Do not ignore failing tests without a documented reason.
* Keep commits focused on one logical change.
* Avoid mixing unrelated changes in a single commit.
* Prefer small, understandable Pull Requests.
* Update documentation when a development or architectural rule changes.
* Use `uv` for all backend dependency management and development commands.
* Do not install project development tools globally when they are available through the project's `uv` environment.
* Keep `pyproject.toml` and `uv.lock` synchronized.
* Install the pre-commit Git hooks after cloning the repository or setting up a new development environment.
* Review autogenerated database migrations before applying them.
* Do not use `docker compose down -v` unless the local PostgreSQL database should be reset.
